model:
  # TheSelective-G checkpoint (Global cross-attention / 1p_all)
  joint_ckpt: './checkpoints/theselective_g.pt'

  # Edge configuration for consistency with training
  cutoff_mode: knn
  knn: 32

sample:
  seed: 2021
  num_samples: 10
  num_steps: 1000
  pos_only: False
  center_pos_mode: protein
  sample_num_atoms: prior